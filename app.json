{
    "name": "houston-api",
    "description": "RESTful APIs for Houston",
    "buildpacks": [{ "url": "heroku/python" }],
    "env": {
      "DJANGO_SETTINGS_MODULE": "houston.settings"
    },
    "environments": {
      "test": {
        "env": {
            "DJANGO_SETTINGS_MODULE": "houston.settings_test"
        },
        "scripts": {
            "test": "./manage.py test --debug-mode"
        }
    }
  }
}
